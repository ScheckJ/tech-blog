{{#each posts}}
  <div class="post">
    <h2><a href="/post/{{id}}">{{title}}</a></h2>
    <p>By {{user.username}} on {{createdAt}}</p>
    <p>{{content}}</p>
  </div>
{{else}}
  <p>No blog posts yet.</p>
{{/each}}
